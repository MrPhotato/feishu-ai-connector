# 贡献指南

欢迎修复问题、完善部署文档、补充测试，以及增加有明确官方接口依据的工具适配。较大改动请先用 Issue 说明具体场景、现有行为和计划变更；安全问题按 [SECURITY.md](SECURITY.md) 私密报告。

## 开发环境

本项目基于妙搭全栈应用及其平台 SDK。请使用自己的开发应用和合成测试数据，不连接未获授权的组织实例。公共源码不包含可复用的生产密钥或平台绑定。

1. 阅读 [部署指南](docs/deployment.md)，准备 Node.js 22 或更新版本及所需平台开发条件。
2. 安装依赖，按 [配置示例](deployment/instance.example.json) 准备被忽略的本地副本。
3. 在独立分支进行修改。不要提交 `.env.local`、实例配置、生成包、平台绑定或调试输出。

## 修改原则

- 将通用逻辑与部署配置分开。不要新增组织域名、个人身份、固定业务对象 ID 或真实业务示例。
- 保留平台管理的启动、鉴权和日志集成。需要改变平台行为时，先确认受支持的扩展方式，不通过关闭保护来让测试通过。
- 后端请求和响应遵循 `shared/` 契约；输入应有明确 schema，错误响应不携带凭据、上游请求或原始业务内容。
- 新增工具时注明用户身份、所需 scope、读写性质和分页边界。scope 必须依据官方声明，不用更宽的权限或应用身份掩盖接口限制。
- 原生目录只能选择既定命令，用户输入不能变成 shell、任意旗标、文件路径或目标 URL。写操作不得自动补造用户确认。
- 新增 API 适配应使用合成回归。测试成功不等于所有应用资格、用户权限或资源范围已通过真实验收。

## 本地验证

按改动范围运行类型检查及相关测试：

```sh
npm run type:check
node scripts/test-deployment-config.mjs
node scripts/test-feishu-task-tools.mjs
node scripts/probe-cli-task-plans.mjs
```

这些测试使用合成数据，不需要真实 OAuth 凭据。若改动涉及 OAuth、存储、日志保护或时区，还应运行相应的本地回归脚本；不要将远程探针混入默认离线测试。

修改 CLI 版本、发行归档或原生目录时，请一起更新固定版本、哈希、目录生成结果和受影响的输出契约测试。不同官方命令可能有不同成功输出格式，不能凭一个命令的结果推广到所有读写操作。

修改插件模板时，保持模板没有实例连接地址，通过配置生成安装包，并校验模板与生成包。生成物、实例配置和开发者个人元数据不进入 PR。

## 提交 Pull Request

请说明：

- 解决的问题和具体触发条件。
- 用户可观察到的行为变化，以及权限或兼容性影响。
- 运行过的检查、未覆盖的验证，以及是否需要独立部署配置。

提交前检查当前差异和新增文件是否含真实姓名、邮箱、业务正文、内部链接、密钥或个人路径。用合成示例替换复现材料；不要把整个工作目录或旧部署的 Git 历史复制到公开分支。

代码合并、妙搭部署、飞书应用权限审核和用户 OAuth 是不同操作。贡献源码不会自动发布服务，也不会替任何用户授予业务权限。

贡献内容应与仓库 [MIT 许可证](LICENSE) 兼容；新增第三方代码或资产时，注明来源并保留其要求的许可声明。
