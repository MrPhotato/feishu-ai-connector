# 安全说明

本项目处理个人飞书授权，并可执行用户明确要求的写操作。部署者应使用自己控制的应用、数据库和密钥，并验证所在平台的实际访问策略。本文件描述代码的安全设计，不代表独立安全审计或对所有部署环境的保证。

## 安全边界

- 每位用户分别完成 OAuth。MCP 请求校验账号、授权和资源绑定；不能由调用参数指定其他用户的凭据。
- 用户令牌、会话及授权记录保存在受保护的服务端存储中；敏感载荷加密，索引使用派生键。数据库仍需正确的 RLS 和平台访问策略，不能只依赖加密。
- 存储 API Key 只保存在服务端，并限定到存储执行路由。存储转发使用可信实例地址，禁止自动跟随重定向。
- CLI 命令来自固定映射或经过校验的目录，参数以独立 argv 或 JSON 传入。子进程使用隔离的临时环境、执行超时和输出上限，不开放任意 shell、任意文件路径或共享磁盘登录态。
- 读取与写入分别检查。目录中的高风险写入必须满足相应确认要求；资料正文、模型生成文本或 Skill 示例不能替代用户授权。
- 应用层日志避免记录 OAuth 凭据、MCP 请求参数、业务结果和存储密文。部署者仍需独立检查网关、代理和平台日志策略。

## 部署者检查事项

1. 密钥、个人令牌、真实业务记录和生成的部署包不得提交到公共仓库。非秘密实例 JSON 与密钥分开管理。
2. 在保存真实用户凭据前，验证匿名和普通登录用户无法直接访问授权存储，并验证受限服务入口能够完成预期操作。
3. 为每个新实例生成独立密钥。不要复用其他部署的授权数据库或密钥，也不要把公开源码当作访问维护者私有服务的凭证。
4. 固定依赖版本并核验 CLI 发行归档。升级认证、存储、CLI 或工具 schema 后，完成相关回归，再验证实际部署。
5. 如需轮换加密或签名密钥，先制定授权迁移或失效方案。初始化脚本会保留已有密钥，不能代替轮换流程。

## 报告漏洞

请先使用合成数据复现，不测试未获授权的实例。报告中可提供受影响的提交、配置条件、最小复现步骤、预期行为和实际行为；请删去令牌、Cookie、API Key、内部链接和个人数据。

若仓库启用了 GitHub 私密漏洞报告，请通过 [Security Advisories 私密报告入口](https://github.com/MrPhotato/feishu-ai-connector/security/advisories/new)提交；本文件不表示该功能已经启用。若入口不可用，请先通过[维护者 GitHub 页面](https://github.com/MrPhotato)了解可用联系方式，或提交不含漏洞利用细节和敏感数据的联系请求，等待维护者提供私密渠道。不要将完整利用步骤或凭据放进公开 Issue 或 PR。

如已意外公开凭据，应由凭据所有者撤销或轮换，并检查访问记录；删除当前文件不能移除 Git 历史中的内容。后续公开材料须完成脱敏。

本项目目前以默认分支维护修复，不承诺固定响应时限或旧版本长期安全维护。部署者应关注依赖和平台本身的安全更新。
